package Class11;

public class Cucumber_Annotations {
    /**
     * Before:
     *      the method with the above annotation will run before every scenario
     *
     *      create a method that can launch website,
     *          and Before annotation on it.
     *
     * After:
     *      the method with the above annotation will run after every scenario
     *
     *      create a method to quit all webpages launched due to automation,
     *          and add After annotation on it.
     *
     */

}
